
mat-card.componente
    h1 {{ contacto?.nombre }}
    form(class="example-form")
            mat-form-field
                input(matInput, placeholder="Busca un nombre", [(ngModel)]="contacto.nombre",  name="nombre")
            mat-form-field
                input(matInput, placeholder="Busca un correo", [(ngModel)]="contacto.correo",  name="correo")
            mat-form-field
                input(matInput, placeholder="Busca un empresa", [(ngModel)]="contacto.empresa",  name="empresa")
            mat-form-field
                input(matInput, placeholder="Busca un mensaje", [(ngModel)]="contacto.mensaje",  name="mensaje")
    button(mat-raised-button (click)="aceptar()") Aceptar
